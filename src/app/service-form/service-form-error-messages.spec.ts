import { ServiceFormErrorMessages } from './service-form-error-messages';

describe('ServiceFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new ServiceFormErrorMessages()).toBeTruthy();
  });
});
